function generate_evaluation_truth()
    arguments(Input)
    end

    % Create Images/EvalTruth if it doesn't exist
    % files = List all png files in Images/Evaluation
    % for each file in files
    %   Build the file path for Images/EvalTruth/imageFile.png
    %   if imageFile does not exist
    %       load eval image file
    %       resize to correct size
    %       perform ICA -- See generate_pixel_labels
    %       Get classes -- See generate_pixel_labels
    %       Create new image file -- See generate_pixel_labels
    %       Save generated image
    %   end
    % end

end